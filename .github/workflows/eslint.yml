name: ESLint

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Install modules
      run: npm install
    - name: Run ESLint
      run: npm run eslint
